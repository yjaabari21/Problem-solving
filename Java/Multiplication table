import java.util.*;
        
public class test {
public static void main(String args[])
{
    int i;
    Scanner scn = new Scanner(System.in);
    int num = scn.nextInt();
    for(i = 1; i<=12; i++){
       int z = num * i;
        System.out.println(num + " * " + i + " = " + z);
  }
 }
}
